// This file is intentionally left blank or can be deleted.
// The route /services is now redirected to /offerings via next.config.ts.
// Keeping it (even empty) or deleting it achieves the same outcome due to the redirect.
// For cleanliness, this file would typically be deleted from the project.
// However, the App Prototyper cannot delete files, so its content is removed.
export default function OldServicesPage() {
  return null;
}
